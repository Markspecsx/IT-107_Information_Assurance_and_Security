# Disable directory listing
Options -Indexes

# Deny direct access to sensitive directories
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(includes|php|js)/ - [R=404,L,NC]
</IfModule>
